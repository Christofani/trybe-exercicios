// const soma = require('./sum');

// console.log(soma(1, 2));

const {subtraction} = require('./operation');

const resultado = subtraction(1, 2);
const resultado2 = subtraction(2, 1);
const resultado3 = subtraction(2, 2);

console.log(resultado, resultado2, resultado3);